//import "../global.css";
import { StatusBar } from "expo-status-bar";
import {
  StyleSheet,
  Text,
  ScrollView,
  View,
  TextInput,
  TouchableOpacity,
} from "react-native";
import React, { useState } from "react";

export default function Profile({ navigation }) {
  return (
    <View className="items-center justify-start bg-white-500 pt-32 px-6">
      <Text className="text-3xl font-bold text-green-600">Weclome Back!</Text>
    </View>
  );
}
